# IP da maquina rodando o backend na rede local - NUNCA localhost (no
# celular/emulador isso resolveria pro proprio dispositivo, nao pro seu
# computador). Descubra com `ipconfig` (Windows) ou `ifconfig`/`ip a` (Mac/Linux).
EXPO_PUBLIC_API_URL=http://192.168.0.10:3000
